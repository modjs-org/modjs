import { ModLightTheme } from '@modjs/utils'

const lightTheme = {
    ...ModLightTheme,
    typography: {
        ...ModLightTheme.typography,
        ff: {
            primary: 'Rajdhani',
        },
    },
}

export default lightTheme
